# Handoff — 2026-03-12

## Estado atual (resumo executivo)
- `/agenda-wall` refatorada com cards compactos para TV, status por categoria e jornalista, detalhamento colapsável.
- Cores padronizadas por fonte global (`statusTheme`):
  - verde = no prazo
  - amarelo = no prazo (janela aberta / hora atual)
  - vermelho = fora do prazo
  - azul = acima do combinado
- Texto padronizado para **NO PRAZO** (substituiu EM PRAZO).
- Plugin atualizado para fornecer histórico meta consistente:
  - inclui dias/categorias sem post (`count=0`)
  - inclui `meta_applicable`
  - regras explícitas por portal meta-only.

## Commits-chave do dia
### Frontend (`radar-editorial-mt`)
- `75151cb` regras fallback atualizadas (PPMT/PNMT/ROO/AFL)
- `001593e` EM PRAZO -> NO PRAZO (global UI/docs)
- `24e04a8` categorias/jornalistas com estado `acima`
- `0437725` heatmap com horas perdidas em vermelho + densidade vertical
- `3f5bd7e` `statusTheme` global + meta-only compacto
- `e83d1d9` oculta métricas horárias em portais meta-only

### Plugin (`codigo5-manutencao`)
- `4316222` regras explícitas por portal meta-only
- `e1912f9` `meta_applicable` no feed
- `b18321d` `history.meta` com zero-count

## Regras oficiais hoje
- PMT/OMT: misto (hora + meta)
- ROO: meta-only por categoria
- PNMT: meta-only por categoria
- PPMT: meta-only por categoria
- AFL: meta-only por categoria (**Primavera**, não Rondonópolis)

## Deploy
- Front: `main` de `leandrobosaipo/radar-editorial-mt`.
- Plugin: publicado via SSH/SCP + `wp cron event run radar_editorial_refresh_event` nos 6 portais.

## Validação já feita
- Feed `/wp-json/radar/v1/feed` com `editorial_rules` corretas em ROO/PNMT/PPMT/AFL.
- `history.meta` não vem mais vazio por ausência de posts.

## Próximos passos recomendados (ordem)
1. Aplicar `statusTheme` também no `/tv` (último ponto de consistência visual).
2. Rodar checklist visual em 1366x768 e 1920x1080 para garantir primeira dobra.
3. Criar testes de regressão para cores/status (meta-only e misto).

## Como retomar rápido na próxima sessão
1. Abrir `docs/HANDOFF_2026-03-12.md`
2. Abrir `docs/CHANGELOG_EDITORIAL.md`
3. Validar repo/branch:
   - `pwd`
   - `git remote -v`
   - `git branch --show-current`
4. Rodar local:
   - `npm run dev -- --host 0.0.0.0 --port 4173`
